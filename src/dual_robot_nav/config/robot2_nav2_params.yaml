robot2:
  amcl:
    ros__parameters:
      use_sim_time: True
      base_frame_id: "robot2/base_footprint"
      global_frame_id: "robot2/map"
      odom_frame_id: "robot2/odom"
      scan_topic: /robot2/scan

  controller_server:
    ros__parameters:
      use_sim_time: True
      odom_topic: /robot2/odom

  local_costmap:
    local_costmap:
      ros__parameters:
        use_sim_time: True
        global_frame: robot2/odom
        robot_base_frame: robot2/base_link
        scan:
          topic: /robot2/scan

  global_costmap:
    global_costmap:
      ros__parameters:
        use_sim_time: True
        global_frame: robot2/map
        robot_base_frame: robot2/base_link
        scan:
          topic: /robot2/scan

  bt_navigator:
    ros__parameters:
      use_sim_time: True
      global_frame: robot2/map
      robot_base_frame: robot2/base_link

  planner_server:
    ros__parameters:
      use_sim_time: True

  smoother_server:
    ros__parameters:
      use_sim_time: True

  behavior_server:
    ros__parameters:
      use_sim_time: True
      global_frame: robot2/odom
      robot_base_frame: robot2/base_link

  waypoint_follower:
    ros__parameters:
      use_sim_time: True

  velocity_smoother:
    ros__parameters:
      use_sim_time: True
